import 'package:flutter/material.dart';

class BrandCarosel extends StatelessWidget {
  const BrandCarosel({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
